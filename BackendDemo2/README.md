1. Gerenate package.json
    npm init -y

2.Create server.js

3.Install,import "express" and create HTTP server.Assign port

### connect MongoDB database
               
         REST APi ---->       MongoDb native driver --------------> Monodb serevr

        REST APi ---->         MongoDb ODM tool(mongoose)  ----->Monodb serevr


    a. install mongoose and connect to mongodb server 
    b.create schema of resourse
    c.create model of that schema
    d.perform db operations on that model
    

    error hndling

    running v

